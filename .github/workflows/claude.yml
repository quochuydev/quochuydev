name: Write Plan for File

on:
  issue_comment:
    types: [created]

jobs:
  write-plans:
    if: github.event.issue.title == 'Upwork jobs'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Show comment
        run: echo "Comment ✏️ ${{ github.event.comment.body }}"

      - name: Write plan with Claude
        id: claude
        uses: anthropics/claude-code-action@v1
        env:
          ANTHROPIC_BASE_URL: 'https://api.z.ai/api/anthropic'
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          github_token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
          prompt: /write-plans ${{ github.event.comment.body }}

      - name: Commit generated plan
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git commit -m "📝 Generated plan"
          git push
